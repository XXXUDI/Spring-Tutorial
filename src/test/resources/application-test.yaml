db:
  password: root
  username: root

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: unknown
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: validate